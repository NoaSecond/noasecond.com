/**
 * Main Entry Point
 * Used as a wrapper or for small site-wide initializations.
 */
// main.js is now primarily used to load other modules or can be kept empty if modules are loaded via tags
console.log('Portfolio initialized');
